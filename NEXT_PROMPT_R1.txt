有几个问题：1. 正则里写了 (a+)+b 碰到很长不匹配的文件名直接卡死，进程永远不返回。2. Windows 上 HelloWorld.txt 改小写一直报冲突，同一文件改个大小写不应该算冲突。3. rename 到一半程序崩溃了，已经改好的文件没有任何记录能恢复回去。4. 把 C 盘文件改到 D 盘直接抛了个英文错误看不懂，应该自己兜底处理。5. rollback.json 内容我手动改坏了，直接把文件改到了不存在的路径上，缺少校验。6. Excel 导出 CSV 带个 BOM 头过去，第一列的名字前面多了一个乱码字符。7. 预览撤销模式显示"成功"而不是"预览"，我差点以为已经执行了。检查修复一下。